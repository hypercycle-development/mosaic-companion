{
  "name": "text-stats",
  "version": "1.0.0",
  "description": "WASM tool example for Mosaic Companion",
  "scripts": {
    "bundle": "esbuild src/index.ts --bundle --outfile=dist/bundle.js --platform=node --format=cjs --external:env",
    "build": "npm run bundle && extism-js dist/bundle.js -i node_modules/@extism/js-pdk/types/plugin-interface.d.ts -o text-stats.wasm",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@extism/js-pdk": "^1.1.1",
    "esbuild": "^0.28.0",
    "typescript": "^5.4.0"
  }
}
