{
  "name": "mosaic-browser",
  "version": "1.0.28",
  "description": "Mosaic Browser Application",
  "main": "dist_electron/main.js",
  "type": "module",
  "author": "hypercycle",
  "scripts": {
    "dev": "vite",
    "build:electron": "node esbuild.config.js",
    "build:renderer": "vite build",
    "build": "bun run build:electron && bun run build:renderer",
    "preview": "vite preview",
    "start": "bun run build && electron .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist dist_electron release node_modules/.vite",
    "clean:all": "bun run clean && rm -rf node_modules bun.lockb",
    "build:win": "bun run build && electron-builder --win",
    "build:mac": "bun run build && electron-builder --mac",
    "build:linux": "bun run build && electron-builder --linux",
    "build:all": "bun run build:win && bun run build:mac && bun run build:linux"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "electron-updater": "^6.7.3",
    "lucide-react": "^0.562.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "react-toastify": "^11.0.5"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "@types/react": "^19.2.8",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.2",
    "electron": "^39.3.0",
    "electron-builder": "^26.4.0",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  },
  "build": {
    "appId": "com.mosaic.browser",
    "productName": "Mosaic Browser",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "dist_electron/**/*",
      "package.json"
    ],
    "publish": {
      "provider": "s3",
      "bucket": "mosaic-release",
      "region": "us-east-2",
      "path": "/releases"
    },
    "win": {
      "target": "nsis"
    },
    "mac": {
      "target": "default",
      "identity": "-"
    },
    "linux": {
      "target": "AppImage",
      "category": "Utility"
    }
  }
}
